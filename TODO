Spinner needs to load project from passed id

Dropdown needs a bit of padding

Probably more, but I can't think ahead anymore. 

Need to set a default project in the settings. Not right now. will be done later

